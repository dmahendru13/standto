var Markdown = require("markdown-it");

!function() {
    "use strict";
    document.querySelector(".left-column.archived-standto").textContent, document.querySelector(".byline");
    console.log(Markdown.toHTML("**test**")), console.log(Markdown.toHTML("{{content.body}}"));
}();